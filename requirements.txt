# Pinned to the exact versions the pipeline was built and validated against,
# so `docker compose up` reproduces local results bit-for-bit.

# --- core data / ML ---
pandas==2.2.3
numpy==2.4.6
scikit-learn==1.5.2
lightgbm==4.7.0
shap==0.51.0
joblib==1.4.2

# --- explainability / plots ---
matplotlib==3.11.0
seaborn==0.13.2

# --- LLM ---
anthropic==1.3.0

# --- SQL guardrails ---
sqlparse==0.6.0

# --- app ---
streamlit==1.40.2

# --- config / infra ---
pydantic==2.12.5
pydantic-settings==2.14.1
python-dotenv==1.0.1

# --- notebooks / tests ---
nbformat==5.10.4
nbconvert==7.17.1
jupyter==1.1.1
pytest==9.1.1

# --- parquet cache ---
pyarrow==20.0.0
